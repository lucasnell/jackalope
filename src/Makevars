# C++11 syntax used throughout
CXX_STD = CXX11

## Add these to your global Makevars if you want to build with OpenMP
## The global Makevars is located inside the home directory at `.R/Makevars`
# PKG_CXXFLAGS += $(SHLIB_OPENMP_CXXFLAGS)
# PKG_CFLAGS += $(SHLIB_OPENMP_CFLAGS)
# PKG_LIBS += $(SHLIB_OPENMP_CFLAGS)


# For htslib headers:
RHTSLIB_LIBS=`echo 'Rhtslib::pkgconfig("PKG_LIBS")'| "${R_HOME}/bin/R" --vanilla --slave`
# For portable zlib libraries
ZLIBBIOC_CFLAGS = `echo 'zlibbioc::pkgconfig("PKG_CFLAGS")'| "${R_HOME}/bin/R" --vanilla --slave`
ZLIBBIOC_LIBS = `echo 'zlibbioc::pkgconfig("PKG_LIBS_shared")'| "${R_HOME}/bin/R" --vanilla --slave`

# Access to pgc RNG header files
PKG_CPPFLAGS = -I../inst/include/

# Compression library and others
PKG_LIBS = -llzma $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(RHTSLIB_LIBS) $(ZLIBBIOC_LIBS)
# zlib
TMP_CFLAGS = $@(ZLIB_CFLAGS)
ZLIB_CFLAGS = $(TMP_CFLAGS) $(ZLIBBIOC_CFLAGS)
